__global__ void add_100 ( int numElements, int * data ) { int idx = blockDim. x * blockIdx. x + threadIdx. x ; if ( idx < numElements ) data [ idx ] += 100 ; }
__global__ void get_ev ( double * old_arr, double * new_arr ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < size ) new_arr [ tid ] = old_arr [ tid ] ; }
__global__ void square ( int * array, int arrayCount ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < arrayCount ) { array [ idx ] *= array [ idx ] ; } }
__global__ void add ( int n, float * x, float * y ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < n ) y [ i ] = x [ i ] + y [ i ] ; }
__global__ void scale_host ( float * array, float scale, int N ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < N ) { array [ idx ] *= scale ; } }
__global__ void allAddInplace_gpu ( double * arr, double alpha, int n ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < n ) { arr [ i ] += alpha ; } }
__global__ void memsetCpuInt ( int * data, int val, int N ) { int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index < N ) data [ index ] = val ; }
__global__ void initialArray0_gpu ( int tasks, int * f3 ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < tasks ) { f3 [ i ] = 0 ; } }
__global__ void add_vector_gpu ( float * a, float * b, float * c, int size ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < size ) c [ i ] = a [ i ] + b [ i ] ; }
__global__ void test_gpu ( float * input, const int dims ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < dims ) { if ( tid == 0 ) input [ tid ] = 0 ; } }
__global__ void set_sorting_offset ( const int nrows, const int ncols, int * offsets ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid <= ncols ) offsets [ tid ] = tid * nrows ; return ; }
__global__ void dot_gpu ( float * c, float * a, float * b ) { int t_id = blockIdx. x * blockDim. x + threadIdx. x ; if ( t_id < size ) c [ t_id ] = a [ t_id ] * b [ t_id ] ; }
__global__ void matDiagAddInplace_gpu ( double * mat, double alpha, int dim ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < dim ) { mat [ i * dim + i ] += alpha ; } }
__global__ void gpuAddCorrAndCorrection ( float * L, float * r, int N ) { int u = blockIdx. x * blockDim. x + threadIdx. x ; if ( u < N ) L [ u ] -= r [ u ] ; }
__global__ void fill_gpu ( int N, float ALPHA, float * X, int INCX ) { int i = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( i < N ) X [ i * INCX ] = ALPHA ; }
__global__ void scal_gpu ( int N, float ALPHA, float * X, int INCX ) { int i = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( i < N ) X [ i * INCX ] *= ALPHA ; }
__global__ void PSIfill_gpu ( float * array, int conv_length, int n ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < n ) { array [ i ] = array [ i % conv_length ] ; } }
__global__ void host_add ( float * c, float * a, float * b, int n ) { int k = blockIdx. x * blockDim. x + threadIdx. x ; if ( k < n ) c [ k ] = a [ k ] + b [ k ] ; }
__global__ void mul_Scalar_matrix ( float * a, float value, float * c, int N ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < N ) { c [ idx ] = a [ idx ] * value ; } }
__global__ void initWith_gpu ( float num, float * a, int N ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < N ) { a [ i ] = num ; } }
__global__ void zeroIndices_gpu ( long * vec_out, const long N ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < N ) { vec_out [ idx ] = vec_out [ idx ] - vec_out [ 0 ] ; } }
__global__ void saxpy_serial ( const int dim, float a, float * x, float * y ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < dim ) y [ i ] += a * x [ i ] ; }
__global__ void getCanBusData ( int * canData, int size ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < size ) canData [ idx ] += 1 ; }
__global__ void sum_array_gpu ( float * a, float * b, float * c, const int size ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < size ) { c [ i ] = a [ i ] + b [ i ] ; } }
__global__ void matColMeanDiv_gpu ( double * buf, int m, int n, double * tmp ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < n ) { buf [ i ] = tmp [ i ] / m ; } }
__global__ void dmul_Scalar_matrix ( double * a, double value, double * c, int N ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < N ) { c [ idx ] = a [ idx ] * value ; } }
__global__ void countRangesGlobal ( int size, int * A, int * B ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < size ) { int x = A [ i ] / 100 ; B [ x ] += 1 ; } }
__global__ void dsubtract_matrix ( double * a, double * b, double * c, int N ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < N ) { c [ idx ] = a [ idx ] - b [ idx ] ; } }
__global__ void add_arrays ( int n, float * x, float * y, float * z ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < n ) z [ i ] = x [ i ] + y [ i ] ; }
__global__ void sum_arrays_gpu ( int * a, int * b, int * c, int size ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < size ) { c [ i ] = a [ i ] + b [ i ] ; } }
__global__ void iKernel_gpu ( float * A, float * B, float * C, const int N ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < N ) { C [ i ] = A [ i ] + B [ i ] ; } }
__global__ void multiplyIntValues ( int * destination, int * vector, int value, unsigned int end ) { unsigned int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < end ) destination [ i ] = vector [ i ] * value ; }
__global__ void doubleArrayScalarDivide_gpu ( double * d_in, int * d_out, int length, double scalar ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < length ) { d_out [ idx ] = d_in [ idx ] / scalar ; } }
__global__ void add ( const int x, const int y, const int WIDTH, int * c, const int * a, const int * b ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < WIDTH ) c [ i ] = a [ i ] + b [ i ] ; }
__global__ void activate_array_leaky_kernel ( float * x, int n ) { int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index < n ) { float val = x [ index ] ; x [ index ] = ( val > 0 )? val : val / 10 ; } }
__global__ void logistic_gpu ( unsigned int n, float a, float * x, float * z ) { int myId = blockIdx. x * blockDim. x + threadIdx. x ; if ( myId < n ) z [ myId ] = a * x [ myId ] * ( 1 - x [ myId ] ) ; }
__global__ void add_kernel ( float * inputleft, float * inputright, float * output, int count ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < count ) output [ idx ] = inputleft [ idx ] + inputright [ idx ] ; }
__global__ void mul_gpu ( int N, float * X, int INCX, float * Y, int INCY ) { int i = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( i < N ) Y [ i * INCY ] *= X [ i * INCX ] ; }
__global__ void pathPlan ( int * devSpeed, int * devSteer, int size ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < size ) { devSpeed [ tid ] += 1 ; devSteer [ tid ] += 1 ; } }
__global__ void mult_add_into_gpu ( int N, float * X, float * Y, float * Z ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < N ) Z [ i ] += X [ i ] * Y [ i ] ; }
__global__ void InitReduction ( int * flags, int voxelCount, int * reduction, int reductionSize ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < reductionSize ) reduction [ tid ] = ( tid < voxelCount )? flags [ tid ] : 0 ; }
__global__ void Function_update_sgd_gpu ( float lr, float * parameter, float * gradient, int size ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < size ) parameter [ i ] -= lr * gradient [ i ] ; }
__global__ void operacionGPU ( float * u, float * lu, float u_m, float u_d, int n ) { int idx = threadIdx. x + blockIdx. x * blockDim. x ; while ( idx < n ) { lu [ idx ] = ( u [ idx ] - u_m ) / u_d ; idx += blockDim. x * gridDim. x ; } }
__global__ void host_add ( float * c, float * a, float * b, int n ) { int k = blockIdx. x * blockDim. x + threadIdx. x ; if ( k < n ) c [ k ] = a [ k ] + b [ k ] ; }
__global__ void squareSerial ( float * d_in, float * d_out, int N ) { unsigned int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < N ) { d_out [ i ] = pow ( d_in [ i ] / ( d_in [ i ] - 2.3 ), 3 ) ; } }
__global__ void doubleArrayVectorAdd_gpu ( double * d_in_a, double * d_in_b, double * d_out, int length ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < length ) { d_out [ idx ] = d_in_a [ idx ] + d_in_b [ idx ] ; } }
__global__ void fill_matrix ( double * const A, const int rows, const int cols ) { int row = blockIdx. y * blockDim. y + threadIdx. y ; int col = blockIdx. x * blockDim. x + threadIdx. x ; if ( ( row < rows ) && ( col < cols ) ) { A [ row * cols + col ] = row ; } }
__global__ void evenoddincrement_gpu ( float * g_data, int even_inc, int odd_inc, int size ) { int tx = blockIdx. x * blockDim. x + threadIdx. x ; if ( tx < size ) { if ( ( tx % 2 ) == 0 ) { g_data [ tx ] += even_inc ; } else { g_data [ tx ] += odd_inc ; } } }
__global__ void copy_gpu ( int N, float * X, int INCX, float * Y, int INCY ) { int i = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( i < N ) Y [ i * INCY ] = X [ i * INCX ] ; }
__global__ void clearLabel ( float * prA, float * ppa, unsigned int num_nodes, float base ) { unsigned int id = blockIdx. x * blockDim. x + threadIdx. x ; if ( id < num_nodes ) { prA [ id ] = base + prA [ id ] * 0.85 ; ppa [ id ] = 0 ; } }
__global__ void delay_kernel_gpu ( int * N_mobil, int * Tau, int dia ) { int id = blockIdx. x * blockDim. x + threadIdx. x ; if ( id < N ) { if ( Tau [ id ] > 0 ) Tau [ id ] = Tau [ id ] - 1 ; } }
__global__ void resetHeap_gpu ( int * heap, int * heapPtr, int numBlock ) { int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index < numBlock ) { if ( index == 0 ) heapPtr [ 0 ] = numBlock - 1 ; heap [ index ] = numBlock - index - 1 ; } }
__global__ void pow_gpu ( int N, float ALPHA, float * X, int INCX, float * Y, int INCY ) { int i = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( i < N ) Y [ i * INCY ] = pow ( X [ i * INCX ], ALPHA ) ; }
__global__ void kComputeActs ( const float * d_nets, float * d_acts, int size ) { int un_idx = threadIdx. x + blockIdx. x * blockDim. x ; if ( un_idx < size ) { float tact = 1.0f / ( 1.0f + expf ( - d_acts [ un_idx ] ) ) ; d_acts [ un_idx ] = tact ; } }
__global__ void transpositionGPU ( int * vector, int * transposed, int size ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( i < size && j < size ) transposed [ i + j * size ] = vector [ j + i * size ] ; }
__global__ void compute_array_square ( float * array, float * outArray, int size ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < size ) outArray [ i ] = array [ i ] * array [ i ] ; }
__global__ void testInt1_gpu ( const int * input, int dims ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < dims ) { int sum ; for ( int i = 0 ; i < 3000 * 4 ; i ++ ) { if ( input [ i ] == 0 ) { sum ++ ; } } } }
__global__ void incKernel ( int * g_out, int * g_in, int N, int inner_reps ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < N ) { for ( int i = 0 ; i < inner_reps ; i ++ ) { g_out [ idx ] = g_in [ idx ] + 1 ; } } }
__global__ void forward_dropout_layer ( int batch, int inputs, float * input, float probability, float * rand, float scale ) { int i = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( i >= batch * inputs ) return ; if ( rand [ i ] < probability ) input [ i ] = 0 ; else input [ i ] *= scale ; }
__global__ void boundaryCorrectIndexes_gpu ( int * d_in, int * d_out, int length, int N ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < length ) { if ( d_in [ idx ] > N ) { d_out [ idx ] = N ; } else { d_out [ idx ] = d_in [ idx ] ; } } }
__global__ void upsweep_scan ( int twod, int N, int * output ) { int twod1 = twod * 2 ; int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx + twod1 - 1 < N ) output [ idx + twod1 - 1 ] += output [ idx + twod - 1 ] ; }
__global__ void Blend_GPU ( unsigned char * aImg1, unsigned char * aImg2, unsigned char * aRS, int width, int height ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < width * height ) aRS [ i ] = ( unsigned char ) ( 0.5 * aImg1 [ i ] + 0.5 * aImg2 [ i ] ) ; }
__global__ void matVecRowSubInplace_gpu ( double * mat, const double * vec, int m, int n ) { int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index < m * n ) { int i = index / n ; int j = index % n ; mat [ i * n + j ] -= vec [ j ] ; } }
__global__ void matVecColAddInplace_gpu ( double * mat, const double * vec, int m, int n ) { int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index < m * n ) { int i = index / n ; int j = index % n ; mat [ i * n + j ] += vec [ i ] ; } }
__global__ void MMDOuterProdComputeWithSum ( float * x_average, int size_x, float * x_outer_prod ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < size_x ) { x_outer_prod [ i ] = x_average [ i ] * x_average [ i ] ; } }
__global__ void saxpy_gpu ( float * vecY, float * vecX, float alpha, int n ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < n ) vecY [ i ] = alpha * vecX [ i ] + vecY [ i ] ; }
__global__ void set_valid_mask_gpu ( const T * score, T score_thr, int * valid_mask, int dims ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < dims ) { if ( score [ tid ] > score_thr ) { valid_mask [ tid ] = 1 ; } else { valid_mask [ tid ] = 0 ; } } }
__global__ void copy_swap ( float * f_in, float * f_target, const int L_x ) { int k_x = blockIdx. x * blockDim. x + threadIdx. x ; if ( k_x >= L_x ) return ; float tempval = 0.f ; tempval = f_in [ k_x ] ; f_in [ k_x ] = f_target [ k_x ] ; f_target [ k_x ] = tempval ; }
__global__ void sum_backward ( float * db, float * dout, int r, int c ) { int j = blockIdx. x * blockDim. x + threadIdx. x ; if ( j < c ) { for ( int i = 0 ; i < r ; i ++ ) db [ j ] += dout [ i * c + j ] ; } }
__global__ void is_repeat ( int N, int * device_input, int * device_output ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < N ) device_output [ idx ] = 0 ; if ( idx + 1 < N && device_input [ idx ] == device_input [ idx + 1 ] ) device_output [ idx ] = 1 ; }
__global__ void kmeans_average ( int * means, int * counts, int BID, int DIM ) { int bid = blockIdx. x ; int tid = threadIdx. x ; if ( bid < BID ) { for ( int i = 0 ; i < DIM ; i ++ ) { if ( counts [ bid ] == 0 ) means [ bid * DIM + tid ] = 0 ; else means [ bid * DIM + tid ] /= counts [ bid ] ; } } }
__global__ void matPerRowDivInplace_gpu ( double * mat, const double * alphas, int m, int n ) { int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index < m * n ) { int i = index / n ; int j = index % n ; mat [ i * n + j ] /= ( alphas [ i ] + 10 * 3 ) ; } }
__global__ void compute_new_means ( float * mx, float * my, const float * sx, const float * sy, const int * c, int size ) { int cluster = blockIdx. x ; int count = max ( 1, c [ cluster ] ) ; if ( cluster < size ) { mx [ cluster ] = sx [ cluster ] / count ; my [ cluster ] = sy [ cluster ] / count ; } }
__global__ void copy_array_d2d ( double * * src, double * * dst, int m, int n ) { int i = blockIdx. x * blockDim. x + threadIdx. x + 1 ; int j = blockIdx. y * blockDim. y + threadIdx. y + 1 ; if ( ( i >= 1 ) && ( i < m + 1 ) && ( j >= 1 ) && ( j < n + 1 ) ) dst [ i ] [ j ] = src [ i ] [ j ] ; }
__global__ void InitCCL ( int labelList [ ], int reference [ ], int width, int height ) { int x = blockIdx. x * blockDim. x + threadIdx. x ; int y = blockIdx. y * blockDim. y + threadIdx. y ; if ( ( x < width ) && ( y < height ) ) { int id = x + y * width ; labelList [ id ] = reference [ id ] = id ; } }
__global__ void gpu_set_sg ( int * sxz, int sxbeg, int szbeg, int jsx, int jsz, int ns, int npml, int nnz ) { int id = blockIdx. x * blockDim. x + threadIdx. x ; if ( id < ns ) sxz [ id ] = nnz * ( sxbeg + id * jsx + npml ) + ( szbeg + id * jsz + npml ) ; }
__global__ void addMatrix ( float * a, float * b, float * c, int N ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( ( i < N ) && ( j < N ) ) { int idx = i * N + j ; a [ idx ] = b [ idx ] + c [ idx ] ; } }
__global__ void resizedClsScore_gpu ( const T * score, const T * score_factors, T * output, int dims ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < dims ) { if ( score [ tid ] == ( -1 ) ) { output [ tid ] = -1 ; } else { output [ tid ] = score [ tid ] * score_factors [ tid ] ; } } }
__global__ void l1_gpu ( int n, float * pred, float * truth, float * delta, float * error ) { int i = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( i < n ) { float diff = truth [ i ] - pred [ i ] ; error [ i ] = fabs ( diff ) ; delta [ i ] = diff > 0? 1 : -1 ; } }
__global__ void AddMatrixOnGPU ( int * A, int * B, int * C, int nx, int ny ) { int i, j ; int cnt = 0 ; for ( j = blockIdx. x * blockDim. x + threadIdx. x ; j < ny ; j += blockDim. x * gridDim. x ) { i = j / nx ; j = j % nx ; C [ cnt ] = A [ cnt ] + B [ cnt ] ; cnt ++ ; } }
__global__ void LreluForward ( T * srcData, T * dstData, int data_size, T alpha ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < data_size ) { dstData [ i ] = srcData [ i ] > 0? srcData [ i ] : srcData [ i ] * alpha ; } }
__global__ void filterFFT_gpu ( float * FFT, float * filter, int nxprj2, int nviews, float scale ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( i < nviews && j < nxprj2 ) { FFT [ i * nxprj2 + j ] *= filter [ i * nxprj2 + j ] * scale ; } }
__global__ void convertFloatToRGBA_gpu ( char * out_image, const float * in_image, int width, int height ) { int x = blockIdx. x * blockDim. x + threadIdx. x ; int y = blockIdx. y * blockDim. y + threadIdx. y ; if ( x < width && y < height ) { char temp ; int IND = y * width + x ; float val = in_image [ IND ] ; temp = 255 ; out_image [ IND ] = temp ; } }
__global__ void convertEdgeMaskToFloatCpu ( float * d_output, unsigned char * d_input, unsigned int width, unsigned int height ) { int x = blockIdx. x * blockDim. x + threadIdx. x ; int y = blockIdx. y * blockDim. y + threadIdx. y ; if ( ( x < width ) && ( y < height ) ) { d_output [ y * width + x ] = min ( d_input [ y * width + x ], d_input [ width * height + y * width + x ] ) ; } }
__global__ void gpu_matrix_transpose ( int * mat_in, int * mat_out, unsigned int rows, unsigned int cols ) { unsigned int idx = blockIdx. x * blockDim. x + threadIdx. x ; unsigned int idy = blockIdx. y * blockDim. y + threadIdx. y ; if ( idx < cols && idy < rows ) { unsigned int pos = idy * cols + idx ; unsigned int trans_pos = idx * rows + idy ; mat_out [ trans_pos ] = mat_in [ pos ] ; } }
__global__ void LreluBackward ( float * srcDiff, float * dstDiff, float * srcData, int data_size, float alpha ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < data_size ) { dstDiff [ i ] = ( srcData [ i ] > 0 )? srcDiff [ i ] * 1.0 : srcDiff [ i ] * alpha ; } }
__global__ void gpuReduce ( int * N, const int size ) { if ( size == 1 ) return ; int stride = size / 2 ; for ( int i = blockIdx. x * blockDim. x + threadIdx. x ; i < stride ; i += blockDim. x * gridDim. x ) N [ i ] += N [ i + stride ] ; return ; }
__global__ void devidecountInnerGPU ( long Xsize, long Ysize, long Zsize, double * p, double * pn, int * pcountinner ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < Xsize * Ysize * Zsize ) { if ( pcountinner [ tid ] > 1 ) { p [ tid ] = pn [ tid ] / pcountinner [ tid ] ; pn [ tid ] = 0 ; } } }
__global__ void gpuConvertToBits ( int * bit_decisions, unsigned short * bit_stream, int dec_size ) { int dec_index = blockIdx. x * blockDim. x + threadIdx. x ; if ( dec_index >= dec_size ) return ; int bit_index = dec_index * 2 ; int curr_decision = bit_decisions [ dec_index ] ; bit_stream [ bit_index ] = ( ( curr_decision & 2 ) >> 1 ) ; bit_stream [ bit_index + 1 ] = ( curr_decision & 1 ) ; }
__global__ void copyAliasRow ( int * devMat, int memWidth, int memHeight, int size ) { int devMatX = blockIdx. x * blockDim. x + threadIdx. x ; if ( devMatX < size ) { devMat [ memWidth * 0 + devMatX ] = devMat [ memWidth * ( memHeight - 2 ) + devMatX ] ; devMat [ memWidth * ( memHeight - 1 ) + devMatX ] = devMat [ memWidth * 1 + devMatX ] ; } }
__global__ void ObjFeatures_circularity ( const int compCount, const int * areaRes, const double * perimeter ) { if ( compCount > 0 ) { double * circ = ( double * ) malloc ( compCount * sizeof ( double ) ) ; for ( int i = blockIdx. x * blockDim. x + threadIdx. x ; i < compCount ; i += blockDim. x * gridDim. x ) { circ [ i ] = ( 4.0 * 3.14159265359 * ( double ) areaRes [ i ] ) / ( perimeter [ i ] * perimeter [ i ] ) ; } return ; } return ; }
__global__ void devidecountGPU ( long Xsize, long Ysize, long Zsize, double * pint, int * pcount ) { int n = Xsize * Ysize * 2 + ( Ysize - 2 ) * Zsize * 2 + ( Xsize - 2 ) * ( Zsize - 2 ) * 2 ; int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < n * n ) { if ( pcount [ tid ] > 1 ) { pint [ tid ] /= pcount [ tid ] ; } } }
__global__ void bubbleSort ( int * p, const int size ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( i < size - 1 && j < size - i - 1 ) { if ( p [ j ] > p [ j + 1 ] ) { int temp = p [ j ] ; p [ j ] = p [ j + 1 ] ; p [ j + 1 ] = temp ; } } }
__global__ void matmul ( int a [ ] [ 100 ], int b [ ] [ 100 ], int c [ ] [ 100 ] ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( i < 100 && j < 100 ) { c [ i ] [ j ] = 0 ; for ( int k = 0 ; k < 100 ; k ++ ) c [ i ] [ j ] += a [ i ] [ k ] * b [ k ] [ j ] ; } }
__global__ void cudaKernel_estimateSnr_gpu ( const scalar_t * corrSum, const int * corrValidCount, const scalar_t * maxval, scalar_t * snrValue, const int size ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; if ( idx < size ) { float mean = ( corrSum [ idx ] - maxval [ idx ] * maxval [ idx ] ) / ( corrValidCount [ idx ] - 1 ) ; snrValue [ idx ] = maxval [ idx ] * maxval [ idx ] / mean ; } }
__global__ void gpu_sgemm ( float * C, float * A, float * B, long size ) { long i = blockIdx. x * blockDim. x + threadIdx. x ; long j = blockIdx. y * blockDim. y + threadIdx. y ; if ( i < size && j < size ) { C [ i * size + j ] += A [ i * size + k ] * B [ k * size + j ] ; } }
__global__ void kernelXor ( unsigned int key, char * input_str_cuda, unsigned char * possible_plaintext_str_cuda, int input_length ) { int id = blockIdx. x * blockDim. x + threadIdx. x ; char * keyCharPtr ; if ( id < input_length ) { int keyIndex = id % 4 ; keyCharPtr = ( char * ) & key ) ; char keyChar = keyCharPtr [ keyIndex ] ; possible_plaintext_str_cuda [ id ] = keyChar ^ input_str_cuda [ id ] ; } }
__global__ void envejecer_kernel_gpu ( int * estado, int * edad, int * pupacion, int * N_mobil, int dia ) { int N = N_mobil [ 0 ] ; int id = blockIdx. x * blockDim. x + threadIdx. x ; if ( id < N ) { if ( dia < 80 || dia > 320 ) { if ( edad [ id ] > pupacion [ id ] ) edad [ id ] ++ ; } else { edad [ id ] ++ ; } } }
__global__ void globalCalculateKernel ( float * c, float * a, float * b ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( i < size && j < size ) { c [ i * size + j ] = sin ( a [ i * size + j ] ) * sin ( a [ i * size + j ] ) + cos ( b [ i * size + j ] ) * cos ( b [ i * size + j ] ) ; } }
__global__ void gpu_matrix_mul ( int * a, int * b, int * c, int N ) { int row = blockIdx. y * blockDim. y + threadIdx. y ; int col = blockIdx. x * blockDim. x + threadIdx. x ; if ( row < N && col < N ) { int sum = 0 ; for ( int i = 0 ; i < N ; i ++ ) { sum += a [ row * N + i ] * b [ i * N + col ] ; } c [ row * N + col ] = sum ; } }
__global__ void grayscale ( unsigned char * input, unsigned char * output, int size ) { unsigned char r, g, b ; int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < size ) { r = input [ 3 * i ] ; g = input [ 3 * i + 1 ] ; b = input [ 3 * i + 2 ] ; output [ i ] = ( unsigned char ) ( 0.21 * ( float ) r + 0.71 * ( float ) g + 0.07 * ( float ) b ) ; } }
__global__ void subtractMean_gpu ( double * images, const double * meanImage, int imageNum, int pixelNum ) { int col = blockIdx. x * blockDim. x + threadIdx. x ; int row = blockIdx. y * blockDim. y + threadIdx. y ; if ( col < pixelNum && row < imageNum ) { images [ row * pixelNum + col ] -= meanImage [ col ] ; if ( images [ row * pixelNum + col ] < 0.0 ) { images [ row * pixelNum + col ] = 0.0 ; } } }
__global__ void kernelMaximum ( float * maxhd, float * maxvd, int start, int size ) { int tx = blockIdx. x * blockDim. x + threadIdx. x ; int max_hd = 1.00000000000000000e-38F ; int max_vd = 1.00000000000000000e-38F ; if ( tx < size ) { if ( maxhd [ tx ] > max_hd ) max_hd = maxhd [ tx ] ; if ( maxvd [ tx ] > max_vd ) max_vd = maxvd [ tx ] ; } }
__global__ void SparseMatmul_forward ( float * a, float * b, float * c, int * indptr, int * indices, int p, int size ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < size - 1 ) { int jj = indptr [ i ] ; int jj1 = indices [ i + 1 ] ; int j = indices [ jj ] ; for ( int k = 0 ; k < p ; k ++ ) c [ i * p + k ] += a [ jj ] * b [ j * p + k ] ; } }
__global__ void vectorMatrixMult ( long int totalPixels, int availablePixels, int outPixelOffset, float * matrix, float * vector, float * out ) { long int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < availablePixels ) { float sum = 0.0 ; for ( long int j = 0 ; j < totalPixels ; j ++ ) { sum += matrix [ i * totalPixels + j ] * vector [ j ] ; } out [ i + outPixelOffset ] = sum ; } }
__global__ void convertKinectDisparityInPlace_gpu ( float * d_disparity, int pitch, int width, int height, float depth_scale ) { int x = blockIdx. x * blockDim. x + threadIdx. x ; int y = blockIdx. y * blockDim. y + threadIdx. y ; if ( ( x < width ) && ( y < height ) ) { float * d_in = ( float * ) ( ( char * ) d_disparity + y * pitch ) + x ; * d_in = ( * d_in == 0.0f )? 1 : ( - depth_scale / * d_in ) ; } }
__global__ void SparseMatmul_backward ( float * a, float * b_grad, float * c_grad, int * indptr, int * indices, int p, int size, float * grad ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i >= size - 1 ) return ; int jj = indptr [ i ] ; int jj1 = indptr [ i + 1 ] ; int jj2 = indices [ jj ] ; for ( int k = 0 ; k < p ; k ++ ) b_grad [ j * p + k ] += c_grad [ i * p + k ] * a [ jj ] ; }
__global__ void subsample_ind_and_labels_gpu ( int * d_ind_sub, const int * d_ind, unsigned int * d_label_sub, const unsigned int * d_label, int n_out, float inv_sub_factor ) { int ind_out = blockIdx. x * blockDim. x + threadIdx. x ; if ( ind_out < n_out ) { int ind_in = ( int ) floorf ( ( float ) ( ind_out ) * inv_sub_factor ) ; d_ind_sub [ ind_out ] = d_ind [ ind_in ] ; d_label_sub [ ind_out ] = d_label [ ind_in ] ; } }
__global__ void mxm_1d_gpu ( double * a, const int m, double * b, const int n, double * c, const int p ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < m ) { for ( int k = 0 ; k < p ; k ++ ) { double s = 0.0 ; for ( int j = 0 ; j < n ; j ++ ) { s += a [ j * m + i ] * b [ k * n + j ] ; } c [ k * m + i ] = s ; } } }
__global__ void fabsf_clamp_gpu ( int N, T * X, int INCX, T clamp_min, T clamp_max ) { int i = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( i < N ) { if ( X [ i * INCX ] >= 0 ) { X [ i * INCX ] = fmin ( clamp_max, fmax ( clamp_min, X [ i * INCX ] ) ) ; } else { X [ i * INCX ] = fmin ( - clamp_min, fmax ( - clamp_max, X [ i * INCX ] ) ) ; } } }
__global__ void gpu_matrix_mult ( int * h_a, int * h_b, int * h_result, int m, int n, int k ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( i < m && j < k ) { int tmp = 0.0 ; for ( int h = 0 ; h < n ; ++ h ) { tmp += h_a [ i * n + h ] * h_b [ h * k + j ] ; } h_result [ i * k + j ] = tmp ; } }
__global__ void MulMatrixOnGPU ( float * A, float * B, float * C, int nx, int ny ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; float sum = 0.0 ; if ( i < nx && j < ny ) { sum = 0.0 ; for ( int k = 0 ; k < nx ; k ++ ) { sum = sum + A [ i * nx + k ] * B [ k * nx + j ] ; } C [ i * nx + j ] = sum ; } }
__global__ void matrixMulHost ( float * h_M, float * h_N, float * h_P, int width ) { int row = blockIdx. y * blockDim. y + threadIdx. y ; int col = blockIdx. x * blockDim. x + threadIdx. x ; if ( row < width && col < width ) { float Pvalue = 0 ; for ( int k = 0 ; k < width ; ++ k ) { Pvalue += h_M [ row * width + k ] * h_N [ k * width + col ] ; } h_P [ row * width + col ] = Pvalue ; } }
__global__ void mmul_gpu ( const T * A, const T * B, T * C, int r1, int c1, int r2, int c2 ) { int idx = blockIdx. x * blockDim. x + threadIdx. x ; int idy = blockIdx. y * blockDim. y + threadIdx. y ; if ( idx < c2 && idy < c1 ) { T temp = 0 ; for ( int i = 0 ; i < c1 ; i ++ ) temp += A [ idy * c1 + i ] * B [ i * c2 + idx ] ; C [ idy * c2 + idx ] = temp ; } }
__global__ void Dot ( float * C, float * A, float * B, const int r, const int c, const int n ) { float temp ; int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( i < r && j < c ) { temp = 0.0 ; for ( int k = 0 ; k < n ; k ++ ) { temp += A [ i * n + k ] * B [ k * c + j ] ; } C [ i * c + j ] = temp ; } }
__global__ void Forwardsub_gpu ( double * RES, double * LS, double * LW, double * LPR, int NI, int NJ, int Start, int J, int n ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < n ) { int IJ = ( ( Start + i ) * NI ) + ( J - ( Start + i ) ) ; RES [ IJ ] = ( RES [ IJ ] - LS [ IJ ] * RES [ IJ - 1 ] - LW [ IJ ] * RES [ IJ - NJ ] ) * LPR [ IJ ] ; } }
__global__ void gpu_rows_dc_offset_remove_layer_kernel ( float * output, float * input, unsigned int width, unsigned int height, unsigned int depth ) { unsigned int channel = blockIdx. x ; unsigned int row = blockIdx. y * blockDim. y + threadIdx. y ; unsigned int column = blockIdx. z * blockDim. z + threadIdx. z ; if ( ( row < height ) && ( column < width ) ) { unsigned int idx = ( channel * height + row ) * width + column ; output [ idx ] = input [ idx ] - input [ idx + 1 ] ; } }
__global__ void gpu_cross_correlate ( float * Isg, float * Iss, float * sp, float * gp, int npml, int nnz, int nnx ) { int i1 = blockIdx. x * blockDim. x + threadIdx. x ; int i2 = blockIdx. y * blockDim. y + threadIdx. y ; if ( i1 < nnz - npml && i2 < nnx - npml ) { int id = i1 + i2 * nnz ; float ps = sp [ id ] ; float pg = gp [ id ] ; Isg [ id ] += ps * pg ; Iss [ id ] += ps * ps ; } }
__global__ void colorConvert ( unsigned char * grayImage, unsigned char * colorImage, int rows, int columns ) { int column = blockIdx. x * blockDim. x + threadIdx. x ; int row = blockIdx. y * blockDim. y + threadIdx. y ; if ( ( column < columns ) && ( row < rows ) ) { int offset = ( column ) + ( columns * row ) ; unsigned char grayValue = 0.07 * colorImage [ offset * 3 ] + 0.71 * colorImage [ offset * 3 + 1 ] + 0.21 * colorImage [ offset * 3 + 2 ] ; grayImage [ offset ] = grayValue ; } }
__global__ void init_image_array_GPU ( unsigned long long int * image, int pixels_per_image ) { int my_pixel = blockIdx. x * blockDim. x + threadIdx. x ; if ( my_pixel < pixels_per_image ) { image [ my_pixel ] = ( unsigned long long int ) ( 0 ) ; my_pixel += pixels_per_image ; image [ my_pixel ] = ( unsigned long long int ) ( 0 ) ; my_pixel += pixels_per_image ; image [ my_pixel ] = ( unsigned long long int ) ( 0 ) ; my_pixel += pixels_per_image ; image [ my_pixel ] = ( unsigned long long int ) ( 0 ) ; } }
__global__ void diffusion ( const double * x0, double * x1, int nx, int ny, double dt ) { auto i = threadIdx. x + blockIdx. x * blockDim. x ; auto j = threadIdx. y + blockIdx. y * blockDim. y ; auto width = nx + 2 ; if ( i >= 1 && i < nx + 1 && j >= 1 && j < ny + 1 ) { auto pos = i + j * width ; x1 [ pos ] = x0 [ pos ] + dt * ( -4. * x0 [ pos ] + x0 [ pos - width ] + x0 [ pos + width ] + x0 [ pos - 1 ] + x0 [ pos + 1 ] ) ; } }
__global__ void compute_b_minus_Rx ( double * out, double * x, double * b, double * cotans, int * neighbors, int meshStride, int n ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < n ) { out [ i ] = b [ i ] ; for ( int iN = 0 ; iN < meshStride ; ++ iN ) { int neighbor = neighbors [ i * meshStride + iN ] ; double weight = cotans [ i * meshStride + iN ] ; out [ i ] += weight * x [ neighbor ] ; } } }
__global__ void binarize_weights ( float * weights, int n, int size, float * binary ) { int f = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( f >= n ) return ; float mean = 0 ; for ( int i = 0 ; i < size ; ++ i ) { mean += fabs ( weights [ f * size + i ] ) ; } mean = mean / size ; for ( int i = 0 ; i < size ; ++ i ) { binary [ f * size + i ] = ( weights [ f * size + i ] > 0 )? mean : - mean ; } }
__global__ void gather_points_kernel ( int b, int c, int n, int m, const scalar_t * points, const int * idx, scalar_t * out ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( i < b && j < m ) { int a = idx [ i * m + j ] ; out [ ( i * c + l ) * m + j ] = points [ ( i * c + l ) * n + a ] ; } }
__global__ void matrix_mult ( int left_rows, int shared_dimensions, int right_columns, float * left, float * right, float * result ) { int row = blockIdx. x * blockDim. x + threadIdx. x ; int column = blockIdx. y * blockDim. y + threadIdx. y ; if ( row < left_rows && column < right_columns ) { result [ row * right_columns + column ] = 0 ; for ( int cell = 0 ; cell < shared_dimensions ; cell ++ ) { result [ row * right_columns + column ] += left [ row * shared_dimensions + cell ] * right [ cell * right_columns + column ] ; } } }
__global__ void matrixMultiplication_gpu ( int * host_a, int * host_b, int * host_c, int row_a, int col_a, int col_b ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; int tmp = 0 ; for ( int k = 0 ; k < col_a ; ++ k ) { tmp += host_a [ i * col_a + k ] * host_b [ j * col_b + k ] ; } host_c [ i * col_b + j ] = tmp ; }
__global__ void Backwardsub ( double * U, double * RES, double * UN, double * UE, double * LPR, int NI, int NJ, int End, int J, int n ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < n ) { int IJ = ( ( End - i ) * NI ) + ( J - ( End - i ) ) ; RES [ IJ ] = RES [ IJ ] - UN [ IJ ] * RES [ IJ + 1 ] - UE [ IJ ] * RES [ IJ + NJ ] ; U [ IJ ] = U [ IJ ] + RES [ IJ ] ; } }
__global__ void convolution_gpu_1d ( const float * input, const float * mask, float * output, int array_size, int mask_size ) { int MASK_RADIUS = mask_size / 2 ; float temp = 0.0f ; int ELEMENT_INDEX = blockIdx. x * blockDim. x + threadIdx. x ; if ( ELEMENT_INDEX < array_size ) { temp = 0 ; for ( int j = 0 ; j < mask_size ; j ++ ) { ELEMENT_INDEX = i - MASK_RADIUS + j ; if (! ( ELEMENT_INDEX < 0 || ELEMENT_INDEX > ( array_size - 1 ) ) ) { temp += input [ ELEMENT_INDEX ] * mask [ j ] ; } } output [ i ] = temp ; } }
__global__ void getRho ( const int numOfNucl, const double * psi, const double * occNo, double * rho, const char debug ) { * rho = 0 ; for ( int i = blockIdx. x * blockDim. x + threadIdx. x ; i < numOfNucl ; i += blockDim. x * gridDim. x ) * rho += occNo [ i ] * psi [ i ] * psi [ i ] ; if ( debug == 1 ) printf ( " DEBUG : ▁ print ▁ of ▁ RHO : \n ▁ RHO : % f \n This ▁ is ▁ the ▁ last line ( RHO ). \n \n ", * rho ) ; }
__global__ void colLog2SumExp2_gpu ( const scalar_t * mat, scalar_t * buf, int m, int n ) { int j = blockIdx. x * blockDim. x + threadIdx. x ; if ( j < n ) { scalar_t maximum = mat [ j ] ; for ( int i = 1 ; i < m ; i ++ ) { if ( mat [ i * n + j ] > maximum ) { maximum = mat [ i * n + j ] ; } } scalar_t res = 0.0 ; for ( int i = 0 ; i < m ; i ++ ) { res += mat [ i * n + j ] - maximum ; } buf [ j ] = res + maximum ; } }
__global__ void bitPrune_gpu ( unsigned char * out, float * in, int frontPrune, int outputlength, int inputLength, int n ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i >= n ) return ; int batch = i / outputlength ; int indexInBatch = i % outputlength ; int batchInJump = batch * inputLength ; int indexOutBatch = i % outputlength ; int batchOutJump = batch * outputlength ; int frontJump = frontPrune ; out [ batchOutJump + indexOutBatch ] = ( unsigned char ) ( in [ batchInJump + frontJump + indexInBatch ] > 0 ) ; }
__global__ void residual ( double * out, double * x, double * b, double * cotans, int * neighbors, double * diag, int meshStride, int n ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < n ) { out [ i ] = diag [ i ] * x [ i ] - b [ i ] ; for ( int iN = 0 ; iN < meshStride ; ++ iN ) { int neighbor = neighbors [ i * meshStride + iN ] ; double weight = cotans [ i * meshStride + iN ] ; out [ i ] -= weight * x [ neighbor ] ; } } }
__global__ void forward_avgpool_layer ( int batch, int c, int h, int w, float * input, float * output ) { int b, i, k ; int out_index = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( out_index >= batch * c * h * w ) return ; b = out_index / ( c * h * w ) ; i = out_index - b * c * h * w ; output [ out_index ] = 0 ; for ( i = 0 ; i < h * w ; ++ i ) { int in_index = i + h * w * ( k + b * c ) ; output [ out_index ] += input [ in_index ] ; } output [ out_index ] /= h * w ; }
__global__ void convolutionColumnGPU ( float * h_Dst, float * h_Src, float * h_Filter, int imageW, int imageH, int filterR ) { int x, y, k ; x = blockIdx. x * blockDim. x + threadIdx. x ; y = blockIdx. y * blockDim. y + threadIdx. y ; if ( x >= imageW || y >= imageH ) return ; float sum = 0 ; for ( k = - filterR ; k <= filterR ; k ++ ) { int d = y + k ; if ( d >= 0 && d < imageH ) { sum += h_Src [ d * imageW + x ] * h_Filter [ filterR - k ] ; } h_Dst [ y * imageW + x ] = sum ; } }
__global__ void matrMult ( float * A, float * B, float * C, int rowsA, int colsA, int colsB ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( i < rowsA && j < colsB ) { for ( int k = 0 ; k < colsA ; ++ k ) { C [ i * colsB + j ] += A [ i * colsA + k ] * B [ k * colsB + j ] ; } } }
__global__ void add_sources_d ( const float * const model, float * wfp, const float * const source_amplitude, const int * const sources_z, const int * const sources_x, const int nz, const int nx, const int nt, const int ns, const int it ) { int x = blockIdx. x * blockDim. x + threadIdx. x ; int b ; if ( x < nx ) { b = sources_z [ b * ns + x ] * nx + sources_x [ b * ns + x ] ; int ib = b * nz * nx + i ; wfp [ ib ] += source_amplitude [ b * ns * nt + x * nt + it ] * model [ i ] ; } }
__global__ void variance_gpu ( float * x, float * mean, int batch, int filters, int spatial, float * variance ) { int i = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( i >= filters ) return ; variance [ i ] = 0 ; for ( int j = 0 ; j < batch ; ++ j ) { for ( int k = 0 ; k < spatial ; ++ k ) { int index = j * filters * spatial + i * spatial + k ; variance [ i ] += powf ( ( x [ index ] - mean [ i ] ), 2 ) ; } } variance [ i ] *= scale ; }
__global__ void grad_y_gpu ( const T * u, T * grad, long depth, long rows, long cols ) { int x = blockIdx. x * blockDim. x + threadIdx. x ; int y = blockIdx. y * blockDim. y + threadIdx. y ; int z = blockIdx. z * blockDim. z + threadIdx. z ; if ( x < cols && y < rows && z < depth ) { unsigned long size2d = rows * cols ; unsigned long long idx = z * size2d + y * cols + x ; T uidx = u [ idx ] ; if ( y - 1 >= 0 ) { grad [ idx ] = ( uidx - u [ z * size2d + ( y - 1 ) * cols + x ] ) ; } } }
__global__ void grad_x_gpu ( const T * u, T * grad, long depth, long rows, long cols ) { int x = blockIdx. x * blockDim. x + threadIdx. x ; int y = blockIdx. y * blockDim. y + threadIdx. y ; int z = blockIdx. z * blockDim. z + threadIdx. z ; if ( x < cols && y < rows && z < depth ) { unsigned long size2d = rows * cols ; unsigned long long idx = z * size2d + y * cols + x ; T uidx = u [ idx ] ; if ( x - 1 >= 0 ) { grad [ idx ] = ( uidx - u [ z * size2d + y * cols + ( x - 1 ) ] ) ; } } }
__global__ void GraphSum_forward ( float * in, float * out, int * indptr, int * indices, int dim, int size ) { int src = blockIdx. x * blockDim. x + threadIdx. x ; if ( src < indptr - 1 ) { for ( int i = indptr [ src ] ; i < indptr [ src + 1 ] ; i ++ ) { int dst = indices [ i ] ; float coef = 1.0 / sqrtf ( ( indptr [ src + 1 ] - indptr [ src ] ) * ( indptr [ dst + 1 ] - indptr [ dst ] ) ) ; for ( int j = 0 ; j < dim ; j ++ ) out [ src * dim + j ] += coef * in [ dst * dim + j ] ; } } }
__global__ void apply_grayscale ( const unsigned char * image, unsigned char * grayimg, int width, int height ) { int x = blockIdx. x * blockDim. x + threadIdx. x ; int y = blockIdx. y * blockDim. y + threadIdx. y ; if ( x < width && y < height ) { const unsigned char R = image [ ( y * width + x ) * 3 + 0 ] ; const unsigned char G = image [ ( y * width + x ) * 3 + 1 ] ; const unsigned char B = image [ ( y * width + x ) * 3 + 2 ] ; unsigned char gray = ( 307 * R + 604 * G + 113 * B ) >> 10 ; grayimg [ y * width + x ] = gray ; } }
__global__ void getOffsetBox_gpu ( const int * clsIndex, const float * max_coordinate, float * offset, int dims, int batchSize, const float * before_nms_boxes ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < dims ) { int numPerbatch = dims ; for ( int i = 0 ; i < batchSize ; i ++ ) { if ( before_nms_boxes [ i * dims * 4 + tid * 4 ] == ( -1 ) ) { offset [ i * numPerbatch + tid ] = 0 ; } else { offset [ i * numPerbatch + tid ] = clsIndex [ i * numPerbatch + tid ] * ( max_coordinate [ i * dims * 4 ] + 1 ) ; } } } }
__global__ void sgemm_kernelGPU ( const float * host_inputArray1, const float * host_inputArray2, float * host_inputArray3, int M, int N, int K, float alpha, float beta ) { int row = blockIdx. y * blockDim. y + threadIdx. y ; int column = blockIdx. x * blockDim. x + threadIdx. x ; if ( row < M && column < N ) { float element_c = 0.f ; for ( int e = 0 ; e < K ; e ++ ) { element_c += host_inputArray1 [ row * K + e ] * host_inputArray2 [ e * N + column ] ; } host_inputArray3 [ row * N + column ] = alpha * element_c + beta * host_inputArray3 [ row * N + column ] ; } }
__global__ void GraphSum_backward ( float * in_grad, float * out_grad, int * indptr, int * indices, int size, int dim ) { int src = blockIdx. x * blockDim. x + threadIdx. x ; if ( src < size - 1 ) { for ( int i = indptr [ src ] ; i < indptr [ src + 1 ] ; i ++ ) { int dst = indices [ i ] ; float coef = 1.0 / sqrtf ( ( indptr [ src + 1 ] - indptr [ src ] ) * ( indptr [ dst + 1 ] - indptr [ dst ] ) ) ; for ( int j = 0 ; j < dim ; j ++ ) in_grad [ src * dim + j ] += coef * out_grad [ dst * dim + j ] ; } } }
__global__ void CDFfunction ( float * median, float * stdvLogNormalFrame, float * MeanLogNormalFrame, unsigned char * currentFrame, int pixelsPerFrame ) { int pixel = blockIdx. x * blockDim. x + threadIdx. x ; if ( pixel < pixelsPerFrame ) { float newvalue ; float x = currentFrame [ pixel ] ; newvalue = - ( ( log ( x ) - median [ pixel ] ) - MeanLogNormalFrame [ pixel ] ) / ( sqrt ( 2.0 ) * stdvLogNormalFrame [ pixel ] ) ; float summ = 0.5f + 0.5f * erf ( newvalue ) ; if ( summ >= 0.3 ) { currentFrame [ pixel ] = ( unsigned char ) 255 ; } else { currentFrame [ pixel ] = ( unsigned char ) 0 ; } } }
__global__ void mul ( float * M, float * N, float * K, float height_M, float width_N, float width_M ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( ( i < height_M ) && ( j < width_N ) ) { float sum = 0 ; for ( int k = 0 ; k < width_M ; k ++ ) { float a = M [ i * ( int ) width_M + k ] ; float b = N [ k * ( int ) width_N + j ] ; sum += a * b ; } K [ i * ( int ) width_N + j ] = sum ; } }
__global__ void softmax_x_ent_gpu ( int n, float * pred, float * truth, float * delta, float * error ) { int i = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( i < n ) { float t = truth [ i ] ; float p = pred [ i ] ; error [ i ] = ( t )? - log ( p ) : 0 ; delta [ i ] = t - p ; } }
__global__ void normalize_img ( double * image, long int image_size, int bands ) { long int i, j ; long int row ; double * D = ( double * ) calloc ( image_size, sizeof ( double ) ) ; for ( i = blockIdx. x * blockDim. x + threadIdx. x ; i < image_size * bands ; i += blockDim. x * gridDim. x ) { D [ i % image_size ] += image [ i ] ; } for ( i = blockIdx. x * blockDim. x + threadIdx. x ; i < image_size ; i += blockDim. x * gridDim. x ) { D [ i ] = powf ( D [ i ] + 1.0e-16, -1 ) ; } for ( i = blockIdx. x * blockDim. x + threadIdx. x ; i < bands ; i += blockDim. x * gridDim. x ) { row = i * image_size ; for ( j = 0 ; j < image_size ; j ++ ) { image [ row + j ] = image [ row + j ] * D [ j ] ; } } free ( D ) ; }
__global__ void permuteData_gpu ( const T * input, T * output, int num, int devideNum, int featureSize, int priorNum, int batchSize ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < num ) { int numPerbatch = num * devideNum * priorNum ; for ( int s = 0 ; s < batchSize ; s ++ ) { for ( int i = 0 ; i < priorNum ; i ++ ) { for ( int j = 0 ; j < devideNum ; j ++ ) { output [ s * numPerbatch + tid * priorNum * devideNum + i * devideNum + j ] = input [ s * numPerbatch + ( i * devideNum * featureSize ) + ( j * featureSize ) + tid ] ; } } } } }
__global__ void gpuSimpleCorrelator ( float * xi, float * xq, float * sr, float * si, int sLength, float * L, int uLength ) { int u = blockIdx. x * blockDim. x + threadIdx. x ; if ( u < uLength ) { float real = 0 ; float imag = 0 ; float a, b, c, d ; for ( int n = u ; n < u + sLength ; n ++ ) { a = xi [ n ] ; b = xq [ n ] ; c = sr [ n - u ] ; d = si [ n - u ] * ( -1 ) ; real += ( a * c ) - ( b * d ) ; imag += ( a * d ) + ( b * c ) ; } L [ u ] = sqrt ( real * real + imag * imag ) ; } }
__global__ void convertKinectDisparityToRegularDisparity_gpu ( float * d_regularDisparity, int d_regularDisparityPitch, const float * d_KinectDisparity, int d_KinectDisparityPitch, int width, int height ) { int x = blockIdx. x * blockDim. x + threadIdx. x ; int y = blockIdx. y * blockDim. y + threadIdx. y ; if ( ( x < width ) && ( y < height ) ) { float d_in = * ( ( float * ) ( ( char * ) d_KinectDisparity + y * d_KinectDisparityPitch ) + x ) ; float d_out = ( d_in == 0.0f )? 1 : - d_in ; * ( ( float * ) ( ( char * ) d_regularDisparity + y * d_regularDisparityPitch ) + x ) = d_out ; } }
__global__ void runFilterCpu ( float * I, float * Q, int samplesLength, float * filter, int filterLength, float * filtered_I, float * filtered_Q, int convLength ) { int sampleIndex = blockIdx. x * blockDim. x + threadIdx. x ; if ( sampleIndex >= convLength ) return ; int index ; float sumI, sumQ ; sumI = 0 ; sumQ = 0 ; for ( int j = sampleIndex - filterLength + 1 ; j <= sampleIndex ; j ++ ) { index = sampleIndex - j ; if ( ( j < samplesLength ) && ( j >= 0 ) ) { sumI += filter [ index ] * I [ j ] ; sumQ += filter [ index ] * Q [ j ] ; } } filtered_I [ sampleIndex ] = sumI ; filtered_Q [ sampleIndex ] = sumQ ; }
__global__ void l2normalize_gpu ( float * x, float * dx, int batch, int filters, int spatial ) { int b, f, i ; int index = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( index >= filters * spatial ) return ; b = index / spatial ; f = index % spatial ; float sum = 0 ; for ( f = 0 ; f < filters ; ++ f ) { int index = b * filters * spatial + f * spatial + i ; sum += powf ( x [ index ], 2 ) ; } sum = sqrtf ( sum ) ; for ( f = 0 ; f < filters ; ++ f ) { int index = b * filters * spatial + f * spatial + i ; x [ index ] /= sum ; dx [ index ] = ( 1 - x [ index ] ) / sum ; } }
__global__ void distanceMatCalc ( long int totalPixels, int availablePixels, int outPixelOffset, int patchSize, float * distMat, float * data, float filtSig ) { long int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i >= availablePixels * totalPixels ) return ; int data_i = i / totalPixels + outPixelOffset ; int data_j = i % totalPixels ; float tmp = 0.0 ; if ( data_i!= data_j ) { for ( int elem = 0 ; elem < patchSize * patchSize ; elem ++ ) { float diff = ( data [ data_i * patchSize * patchSize + elem ] - data [ data_j * patchSize * patchSize + elem ] ) ; tmp += diff * diff ; } tmp = exp ( - tmp / ( filtSig ) ) ; } distMat [ i ] = tmp ; }
__global__ void shortcut_kernel_gpu ( int size, int minw, int minh, int minc, int stride, int sample, int batch, int w1, int h1, int c1, float * add, int w2, int h2, int c2, float * out ) { int id = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( id >= size ) return ; int i = id % minw ; id /= minw ; int j = id % minh ; id /= minh ; int k = id % minc ; id /= minc ; int b = id % batch ; int out_index = i * sample + w2 * ( j * sample + h2 * ( k + c2 * b ) ) ; int add_index = i * stride + w1 * ( j * stride + h1 * ( k + c1 * b ) ) ; out [ out_index ] += add [ add_index ] ; }
__global__ void dot_gpu ( int N, float * X, int INCX, float * Y, int INCY ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < N ) dot_gpu ( X [ i * INCX ], Y [ i * INCY ] ) ; }
__global__ void k_adam_kernel ( float * m, float * v, float * w, const float * d, int max_size, float beta1, float beta2, float beta1_tpower, float beta2_tpower, float learning_rate ) { const float eps = 1e-8 ; int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < max_size ) { float d_temp = d [ i ] ; m [ i ] = m [ i ] * beta1 + d_temp * ( 1 - beta1 ) ; v [ i ] = v [ i ] * beta2 + d_temp * d_temp * ( 1 - beta2 ) ; float m_hat = m [ i ] / ( 1 - beta1_tpower ) ; float v_hat = sqrt ( v [ i ] / ( 1 - beta2_tpower ) ) + eps ; w [ i ] += ( m_hat / v_hat ) * ( - learning_rate ) ; } }
__global__ void convLayer_forward ( int N, int M, int C, int H, int W, int K, float * X, float * Wk, float * Y ) { int n, m, c, h, w, p, q ; int H_out = H - K + 1 ; int W_out = W - K + 1 ; for ( n = blockIdx. x * blockDim. x + threadIdx. x ; n < N ; n += blockDim. x * gridDim. x ) { m = n / ( H_out * W_out ) ; h = ( n - m * H_out * W_out ) / W_out ; w = n - m * W_out * W_out - h * W_out ; Y [ n ] = 0 ; for ( c = 0 ; c < C ; c ++ ) for ( p = 0 ; p < K ; p ++ ) for ( q = 0 ; q < K ; q ++ ) Y [ n ] += X [ n, c, h + p, w + q ] * Wk [ m, c, p, q ] ; } }
__global__ void opL23_gpu ( float * vec, float * vec1, long depth, long rows, long cols ) { int x = blockIdx. x * blockDim. x + threadIdx. x ; int y = blockIdx. y * blockDim. y + threadIdx. y ; int z = blockIdx. z * blockDim. z + threadIdx. z ; if ( x >= cols || y >= rows || z >= depth ) return ; unsigned long long i = z * rows * cols + y * cols + x ; unsigned long long j = z * rows * cols + y * cols ; unsigned long long size2d = cols ; unsigned long size3d = depth * rows * cols + rows * cols + cols ; if ( i + cols + 1 >= size3d ) return ; vec [ i + cols ] = 0.5 * ( vec1 [ i + cols ] + vec1 [ i ] ) ; if ( j + 1 >= size2d ) return ; vec [ j ] = 0.5 * ( vec1 [ j ] ) ; }
__global__ void upsample_gpu ( float * in, int w, int h, int c, int batch, int stride, int forward, float scale, float * out ) { int i, j, k, b ; int index = ( blockIdx. x + blockIdx. y * gridDim. x ) * blockDim. x + threadIdx. x ; if ( index >= w * h * c ) return ; b = index / ( w * h * c ) ; k = index % ( w * h * c ) ; j = index / stride ; i = index % stride ; int in_index = b * w * h * c + k * w * h + ( j / stride ) * w + i / stride ; int out_index = b * w * h * c * stride * stride + k * w * h * stride * stride + j * w * stride + i ; if ( forward ) out [ out_index ] = scale * in [ in_index ] ; else out [ in_index ] += scale * out [ out_index ] ; }
__global__ void rgb2yuv_kernel ( int img_size, unsigned char * gpu_img_in_r, unsigned char * gpu_img_in_g, unsigned char * gpu_img_in_b, unsigned char * gpu_img_out_y, unsigned char * gpu_img_out_u, unsigned char * gpu_img_out_v ) { unsigned char r, g, b ; int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index >= img_size ) return ; r = gpu_img_in_r [ index ] ; g = gpu_img_in_g [ index ] ; b = gpu_img_in_b [ index ] ; gpu_img_out_y [ index ] = ( unsigned char ) ( 0.299 * r + 0.587 * g + 0.114 * b ) ; gpu_img_out_u [ index ] = ( unsigned char ) ( -0.169 * r - 0.331 * g + 0.499 * b + 128 ) ; gpu_img_out_v [ index ] = ( unsigned char ) ( 0.499 * r - 0.418 * g
__global__ void getDRho ( const int numOfNucl, const real * psi, const real * dpsi, const real * occNo, real * drho, const char debug ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i < numOfNucl ) { drho [ 0 ] = drho [ 0 ] + 2 * occNo [ i ] * psi [ i ] * dpsi [ i ] * 0 ; drho [ 1 ] = drho [ 1 ] + 2 * occNo [ i ] * psi [ i ] * dpsi [ i ] * 1 ; drho [ 2 ] = drho [ 2 ] + 2 * occNo [ i ] * psi [ i ] * dpsi [ i ] * 2 ; } if ( debug == 1 ) printf ( " DEBUG ▁ print ▁ of ▁ DRHO : \n ▁ \t % f\t % f\t % f\n This is the last line ( DRHO ). \n \n ", drho [ 0 ], drho [ 1 ], drho [ 2 ] ) ; }
__global__ void opL12_gpu ( float * vec, float * vec1, long depth, long rows, long cols ) { int x = blockIdx. x * blockDim. x + threadIdx. x ; int y = blockIdx. y * blockDim. y + threadIdx. y ; int z = blockIdx. z * blockDim. z + threadIdx. z ; if ( x >= cols || y >= rows || z >= depth ) return ; unsigned long long i = z * rows * cols + y * cols + x ; unsigned long long j = z * rows * cols + y * cols ; unsigned long long size2d = cols ; unsigned long size3d = depth * rows * cols + rows * cols + cols ; if ( i + cols + 1 >= size3d ) return ; vec [ i + 1 ] = 0.25 * ( vec1 [ i + 1 ] + vec1 [ i ] + vec1 [ i + cols + 1 ] + vec1 [ i + cols ] ) ; if ( j + 1 >= size2d ) return ; vec [ j ] = 0.25 * ( vec1 [ j ] + vec1 [ j + cols ] ) ; }
__global__ void gpuBYUSimplified ( float * xi, float * xq, float * sr, float * si, int N, int Lq, float * L ) { int u = blockIdx. x * blockDim. x + threadIdx. x ; if ( u < N ) { float uSum = 0 ; float r_i, r_q, q_i, q_q ; float realPart, imagPart ; for ( int k = 0 ; k <= 7 ; k ++ ) { realPart = 0 ; imagPart = 0 ; for ( int l = 0 ; l < Lq ; l ++ ) { r_i = xi [ u + k * Lq + l ] ; r_q = xq [ u + k * Lq + l ] ; q_i = sr [ l ] ; q_q = si [ l ] * ( -1 ) ; realPart += ( r_i * q_i ) - ( r_q * q_q ) ; imagPart += ( r_i * q_q ) + ( r_q * q_i ) ; } uSum += ( realPart * realPart ) + ( imagPart * imagPart ) ; } L
__global__ void shortcut_gpu ( int batch, int w1, int h1, int c1, float * add, int w2, int h2, int c2, float s1, float s2, float * out ) { int stride = w1 / w2 ; int sample = w2 / w1 ; assert ( stride == h1 / h2 ) ; assert ( sample == h2 / h1 ) ; if ( stride < 1 ) stride = 1 ; if ( sample < 1 ) sample = 1 ; int minw = ( w1 < w2 )? w1 : w2 ; int minh = ( h1 < h2 )? h1 : h2 ; int minc = ( c1 < c2 )? c1 : c2 ; int i, j, k, b ; for ( b = blockIdx. x * blockDim. x + threadIdx. x ; b < batch ; b += gridDim. x * blockDim. x ) { for ( k = blockIdx. y * blockDim. y + threadIdx. y ; k < minc ; k += gridDim. y * blockDim. y ) { for ( j = blockIdx. z * blockDim. z + threadIdx. z ; j < minh
__global__ void get_before_nms_data_gpu ( const scalar_t * boxes, const scalar_t * scores, const int * labels, const int * index, scalar_t * boxes_out, scalar_t * scores_out, int * labels_out, int dims ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < dims ) { if ( index [ tid ] == 0 ) { boxes_out [ tid * 4 + 0 ] = -1 ; boxes_out [ tid * 4 + 1 ] = -1 ; boxes_out [ tid * 4 + 2 ] = -1 ; boxes_out [ tid * 4 + 3 ] = -1 ; scores_out [ tid ] = -1 ; labels_out [ tid ] = -1 ; } else { boxes_out [ tid * 4 + 0 ] = boxes [ tid * 4 + 0 ] ; boxes_out [ tid * 4 + 1 ] = boxes [ tid * 4 + 1 ] ; boxes_out [ tid * 4 + 2 ] = boxes [ tid * 4 + 2 ] ; boxes_out [ tid * 4 + 3 ] = boxes [ tid * 4 + 3 ]
__global__ void im2col_gpu ( float * data_im, int channels, int height, int width, int ksize, int stride, int pad, float * data_col ) { int c, h, w ; int height_col = ( height + 2 * pad - ksize ) / stride + 1 ; int width_col = ( width + 2 * pad - ksize ) / stride + 1 ; int channels_col = channels * ksize * ksize ; for ( c = blockIdx. x * blockDim. x + threadIdx. x ; c < channels_col ; c += blockDim. x * gridDim. x ) { int w_offset = c % ksize ; int h_offset = ( c / ksize ) % ksize ; int c_im = c / ksize / ksize ; for ( h = 0 ; h < height_col ; ++ h ) { for ( w = 0 ; w < width_col ; ++ w ) { int im_row = h_offset + h * stride ; int im_col = w_offset + w * stride ; int col_index = ( c * height_col + h ) * width_col + w ; data_col [ col_index ] = im2col
__global__ void getTopkNum ( const T * inputScore, const int * inputIndex, T * outputScore, int * outputIndex, T threshold, const int dims, const int * anchorIndex, int * classIndex, const int classNum, int batchSize, int totalScoreNum ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid < dims ) { for ( int i = 0 ; i < batchSize ; i ++ ) { if ( inputScore [ i * totalScoreNum + tid ] >= threshold ) { outputScore [ i * dims + tid ] = inputScore [ i * totalScoreNum + tid ] ; outputIndex [ i * dims + tid ] = inputIndex [ i * totalScoreNum + tid ] ; anchorIndex [ i * dims + tid ] = outputIndex [ i * dims + tid ] / classNum ; classIndex [ i * dims + tid ] = outputIndex [ i * dims + tid ] % classNum ; } else { outputScore [ i * dims + tid ] = 0.0f ; outputIndex [ i * dims + tid ] = -1 ; anchorIndex [ i * dims + tid ] = -1 ; classIndex [ i * dims + tid ] = -
__global__ void fractal_gpu ( const int width, const int frames, unsigned char * const pic ) { const int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i >= width * width * frames ) return ; const float Delta = 0.00304f ; const float xMid = -0.055846456f ; const float yMid = -0.668311119f ; const int frame = i / ( width * width ) ; float delta = Delta * powf ( 0.975f, frame ) ; const int col = i % width ; const float xMin = xMid - delta ; const float yMin = yMid - delta ; const float dw = 2.0f * delta / width ; const int row = ( i / width ) % width ; const float cy = yMin + row * dw ; const float cx = xMin + col * dw ; float x = cx ; float y = cy ; float x2, y2 ; int count = 256 ; do { x2 = x * x ; y2 = y * y ; y = 2.0 * x * y + cy ; x = x2 - y2 + cx ; count -- ; } while ( ( count
__global__ void bit8Channels_gpu ( unsigned char * out, unsigned char * in, int channel, int n ) { int i = blockIdx. x * blockDim. x + threadIdx. x ; if ( i >= n ) return ; int firstIndexToGrab = i * 8 ; unsigned char bit0 = ( in [ firstIndexToGrab + 0 ] & 0x01 ) << 0 ; unsigned char bit1 = ( in [ firstIndexToGrab + 1 ] & 0x01 ) << 1 ; unsigned char bit2 = ( in [ firstIndexToGrab + 2 ] & 0x01 ) << 2 ; unsigned char bit3 = ( in [ firstIndexToGrab + 3 ] & 0x01 ) << 3 ; unsigned char bit4 = ( in [ firstIndexToGrab + 4 ] & 0x01 ) << 4 ; unsigned char bit5 = ( in [ firstIndexToGrab + 5 ] & 0x01 ) << 5 ; unsigned char bit6 = ( in [ firstIndexToGrab + 6 ] & 0x01 ) << 6 ; unsigned char bit7 = ( in [ firstIndexToGrab + 7 ] & 0x01 ) << 7 ; unsigned char output = bit7 | bit6 | bit5 | bit4 | bit3 |
__global__ void Match ( int num_points, float * P, float * Q, int q_points, int * idx, int start, int end ) { float dist ; float max_dist = 1000000000.0f ; int i = blockIdx. x * blockDim. x + threadIdx. x ; int j = blockIdx. y * blockDim. y + threadIdx. y ; if ( i >= start && i < end && j >= start && j < num_points ) { dist = ( P [ 0 + i * 3 ] - Q [ 0 + j * 3 ] ) * ( P [ 0 + i * 3 ] - Q [ 0 + j * 3 ] ) + ( P [ 1 + i * 3 ] - Q [ 1 + j * 3 ] ) * ( P [ 1 + i * 3 ] - Q [ 1 + j * 3 ] ) + ( P [ 2 + i * 3 ] - Q [ 2 + j * 3 ] ) * ( P [ 2 + i * 3 ] - Q [ 2 + j * 3 ] ) ; if ( dist < max_dist ) { max_dist = dist ; idx [ i ] = j ; } } }
__global__ void col2im_gpu ( float * data_col, int channels, int height, int width, int ksize, int stride, int pad, float * data_im ) { int c, h, w ; int height_col = ( height + 2 * pad - ksize ) / stride + 1 ; int width_col = ( width + 2 * pad - ksize ) / stride + 1 ; int channels_col = channels * ksize * ksize ; for ( c = blockIdx. x * blockDim. x + threadIdx. x ; c < channels_col ; c += blockDim. x * gridDim. x ) { int w_offset = c % ksize ; int h_offset = ( c / ksize ) % ksize ; int c_im = c / ksize / ksize ; for ( h = 0 ; h < height_col ; ++ h ) { for ( w = 0 ; w < width_col ; ++ w ) { int im_row = h_offset + h * stride ; int im_col = w_offset + w * stride ; int col_index = ( c * height_col + h ) * width_col + w ; float val = data_col [ col_index ] ;
__global__ void yuv2rgb_kernel ( int img_size, unsigned char * gpu_img_in_y, unsigned char * gpu_img_in_u, unsigned char * gpu_img_in_v, unsigned char * gpu_img_out_r, unsigned char * gpu_img_out_g, unsigned char * gpu_img_out_b ) { int rt, gt, bt ; int rt2, gt2, bt2 ; int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index >= img_size ) return ; rt = ( int ) ( gpu_img_in_y [ index ] + 1.402 * ( gpu_img_in_v [ index ] - 128 ) ) ; gt = ( int ) ( gpu_img_in_y [ index ] - 0.344 * ( gpu_img_in_u [ index ] - 128 ) - 0.714 * ( gpu_img_in_v [ index ] - 128 ) ) ; bt = ( int ) gpu_img_in_y [ index ] + 1.772 * ( gpu_img_in_u [ index ] - 128 ) ; rt2
__global__ void get_boxes_for_nms_gpu ( const float * boxes_before_nms, const float * offset, float * boxes_for_nms, int dims ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid >= dims ) return ; if ( boxes_before_nms [ tid * 4 + 0 ] == ( -1 ) && boxes_before_nms [ tid * 4 + 1 ] == ( -1 ) && boxes_before_nms [ tid * 4 + 2 ] == ( -1 ) && boxes_before_nms [ tid * 4 + 3 ] == ( -1 ) ) { boxes_for_nms [ tid * 4 + 0 ] = ( -1 ) ; boxes_for_nms [ tid * 4 + 1 ] = ( -1 ) ; boxes_for_nms [ tid * 4 + 2 ] = ( -1 ) ; boxes_for_nms [ tid * 4 + 3 ] = ( -1 ) ; } else { boxes_for_nms [ tid * 4 + 0 ] = boxes_before_nms [ tid * 4 + 0 ] + offset [ tid ] ; boxes_for_nms [ tid * 4 +
__global__ void eltwise_gpu ( int batch, int w1, int h1, int c1, float * add, int w2, int h2, int c2, float * out, int sum, int mult ) { int stride = w1 / w2 ; int sample = w2 / w1 ; assert ( stride == h1 / h2 ) ; assert ( sample == h2 / h1 ) ; if ( stride < 1 ) stride = 1 ; if ( sample < 1 ) sample = 1 ; int minw = ( w1 < w2 )? w1 : w2 ; int minh = ( h1 < h2 )? h1 : h2 ; int minc = ( c1 < c2 )? c1 : c2 ; int i, j, k, b ; if ( mult == 1 ) { for ( b = blockIdx. x ; b < batch ; b += gridDim. x ) { for ( k = blockIdx. y ; k < minc ; k += gridDim. y ) { for ( j = threadIdx. x ; j < minh ; j += blockDim. x ) { for ( i = threadIdx. y ; i < minw ; i += blockDim. y
__global__ void decode_gpu ( const scalar_t * anchor, const scalar_t * locData, scalar_t * predictBox, int dims, scalar_t scaleClamp, int batchSize ) { int tid = blockIdx. x * blockDim. x + threadIdx. x ; if ( tid >= dims ) return ; for ( int i = 0 ; i < batchSize ; i ++ ) { scalar_t anchorW = anchor [ i * dims * 4 + tid * 4 + 2 ] - anchor [ i * dims * 4 + tid * 4 ] ; scalar_t anchorH = anchor [ i * dims * 4 + tid * 4 + 3 ] - anchor [ i * dims * 4 + tid * 4 + 1 ] ; scalar_t anchorCx = anchor [ i * dims * 4 + tid * 4 ] + 0.5 * anchorW ; scalar_t anchorCy = anchor [ i * dims * 4 + tid * 4 + 1 ] + 0.5 * anchorH ; scalar_t dx = locData [ i * dims * 4 + tid * 4 ] ; scalar_t dy = locData [ i * dims * 4 + tid * 4 + 1 ] ; scalar_t dw = locData [ i
__global__ void nlf_down_forward_gpu ( const int n, const Dtype * filters, const int channel, const int height, const int width, const int wsize, Dtype * top_data ) { int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index >= n ) return ; int step = height * width ; int base = index * step ; int fbase = index / channel * wsize * step ; for ( int row = 0 ; row < height ; row ++ ) { for ( int col = 0 ; col < width ; col ++ ) { Dtype temp = 0 ; int r = row ; int c = col ; int shift = 0 * step + row * width + col ; temp += top_data [ base + r * width + c ] * filters [ fbase + shift ] ; r = row - 1 ; c = col ; shift = 1 * step + row * width + col ; if ( r >= 0 && c >= 0 ) temp += top_data [ base + r * width + c ] * filters [ fbase + shift ] ; else temp += top_data [ base + row * width + col ] * filters [ fbase + shift ] ; r = row - 1
__global__ void nlf_filter_left_backward_gpu ( const int n, const Dtype * bottom_data, const Dtype * top_data, const Dtype * temp_diff, const int channel, const int height, const int width, const int wsize, Dtype * filters_diff ) { int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index >= n ) return ; int step = height * width ; int base = index / step * step * channel + index % step ; int fbase = index / step * step * wsize + index % step ; int row = index % step / width ; int col = index % step % width ; for ( int i = 0 ; i < channel ; i ++ ) { filters_diff [ fbase ] += temp_diff [ base + i * step ] * bottom_data [ base + i * step ] ; if ( col + 1 < width ) filters_diff [ fbase + step ] += temp_diff [ base + i * step ] * top_data [ base + 1 + i * step ] ; else filters_diff [ fbase + step ] += temp_diff [ base + i * step ] * bottom_data [ base + i *
__global__ void nlf_filter_down_backward_gpu ( const int n, const Dtype * bottom_data, const Dtype * top_data, const Dtype * temp_diff, const int channel, const int height, const int width, const int wsize, Dtype * filters_diff ) { int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index >= n ) return ; int step = height * width ; int base = index / step * step * channel + index % step ; int fbase = index / step * step * wsize + index % step ; int row = index % step / width ; int col = index % step % width ; for ( int i = 0 ; i < channel ; i ++ ) { filters_diff [ fbase ] += temp_diff [ base + i * step ] * bottom_data [ base + i * step ] ; if ( row - 1 >= 0 ) filters_diff [ fbase + step ] += temp_diff [ base + i * step ] * top_data [ base - width + i * step ] ; else filters_diff [ fbase + step ] += temp_diff [ base + i * step ] * bottom_data [ base + i *
__global__ void nlf_up_forward_gpu ( const int n, const Dtype * filters, const int channel, const int height, const int width, const int wsize, Dtype * top_data ) { int index = blockIdx. x * blockDim. x + threadIdx. x ; if ( index >= n ) return ; int step = height * width ; int base = index * step ; int fbase = index / channel * wsize * step ; for ( int row = height - 1 ; row >= 0 ; row -- ) { for ( int col = width - 1 ; col >= 0 ; col -- ) { Dtype temp = 0 ; int r = row ; int c = col ; int shift = 0 * step + row * width + col ; temp += top_data [ base + r * width + c ] * filters [ fbase + shift ] ; r = row + 1 ; c = col ; shift = 1 * step + row * width + col ; if ( r < height ) temp += top_data [ base + r * width + c ] * filters [ fbase + shift ] ; else temp += top_data [ base + row * width + col ] * filters [ fbase + shift ] ; r = row + 1